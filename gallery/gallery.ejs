```{=html}
<div class="quarto-listing quarto-listing-container-grid">
  <div class="list grid quarto-listing-cols-3" style="height: 30px; column-gap: 10px;">
      <% for (const tile of items) { %>
      <div class="card rounded-3 p-2 g-col-1  bg-light" <%= metadataAttrs(tile) %>>
          <a href="<%- tile.url || tile.path %>">
              <img src="<%- tile.image %>" alt="<%- tile.description %>" class="card-img-top border rounded-3 gallery-img"/>
          </a>
          <div class="card-body m-0 px-2 d-flex flex-column justify-content-between">
              <div class="card-title pt-2">
                  <a href="<%- tile.url || tile.path %>"><%= tile.title %></a>
              </div>
              <div class="card-text align-items-end"><small><%= tile.description %></small></div>
          </div>
      </div>
      <% } %>
  </div>
</div>
```